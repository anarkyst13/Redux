import React from 'react';
import { ComponentMeta, ComponentStory } from '@storybook/react';
import { {{pascalCase name}} } from '../ui/{{pascalCase name}}';

export default {
  title: '{{type}}/{{pascalCase name}}',
  component: {{pascalCase name}},
  tags: ['autodocs'],
  argTypes: {},
} as ComponentMeta<typeof {{pascalCase name}}>;

const Template: ComponentStory<typeof {{pascalCase name}}> = (args) => <{{pascalCase name}} {...args} />;

export const Default = Template.bind({});
Default.args = {
  // Пример пропсов по умолчанию
};